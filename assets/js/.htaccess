# Force JS MIME type — critical for Hostinger
<IfModule mod_mime.c>
  AddType application/javascript .js
</IfModule>
<IfModule mod_authz_core.c>
  Require all granted
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Allow from all
</IfModule>
Options -Indexes
