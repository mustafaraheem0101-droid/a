# Force CSS MIME type — critical for Hostinger
<IfModule mod_mime.c>
  AddType text/css .css
</IfModule>
<IfModule mod_authz_core.c>
  Require all granted
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Allow from all
</IfModule>
Options -Indexes
